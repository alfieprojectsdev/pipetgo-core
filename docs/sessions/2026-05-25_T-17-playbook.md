
  ---
  T-17 — PESONet virtual account integration
  
  Plan document: plans/T-17-pesonet-virtual-account.md
  Branch: feat/T17-pesonet-virtual-account
  Estimated sessions: 3 | Estimated wall time: 4–5 hours

  ---
  Session 1 — Explore + Plan (90 min)
  
  Goal: Understand the Xendit VA API, resolve the open payment-research gate, produce the
   plan file.

  Step 1 — Branch (skip; plan is written on main, branch created at execution time)

  Step 2 — Explore (run before calling the planner)

  Say:
  Read src/lib/payments/webhook-auth.ts, src/lib/payments/types.ts, src/features/payments/webhooks/route.ts, and src/features/payments/webhooks/types.ts. Then read plans/T-14-payment-provider-normalization.md section "Decision Log" entries DL-001, DL-003, DL-005. Summarise the normalization boundary contract and the idempotency key namespace convention so the plan picks up where T-14 left off.

  Then separately, resolve the payment-research gate (Xendit VA docs — which API creates a virtual account per order, what the inbound payment webhook shape looks like, and whether the auth mechanism is a static token or HMAC). This is external research; you may need to WebFetch Xendit's developer docs.

  Step 3 — Write the plan

  Use your planner skill to write a plan to plans/T-17-pesonet-virtual-account.md

  The planner runs the interview → steelman → TW + QR gate cycle. Expect 2–4 rounds before the plan passes. Key decisions the plan must resolve:
  - Xendit VA API endpoint + webhook event shape (differs from invoice webhook)
  - Auth mechanism for the VA payment notification (static token vs HMAC — document in webhook-auth.ts if new verifier needed)
  - Idempotency key namespace: xendit:va:PAID:{externalId} format (T-14 DL-005 deferred this to T-17)
  - Whether processPaymentCapture in handlers.ts needs any change (answer must be no — if the plan requires handler edits, that is a plan defect)
  - UI surface: where the virtual account number is displayed on order-detail (T-14 normalized the backend; T-17 adds the UI variant)

  Step 4 — Mandatory context clear

  /clear

  All context must be in the plan file. Do not carry conversation state into Session 2.

  ---
  Session 2 — Implement part 1 (90 min)

  Goal: Provider client + webhook route + auth verifier. Working normalization boundary, no tests yet.

  Step 5 — Branch

  git checkout main && git pull && git checkout -b feat/T17-pesonet-virtual-account

  Step 6 — Execute plan (part 1)

  Use your planner skill to execute plans/T-17-pesonet-virtual-account.md

  Planner state dir convention (use a timestamped subdir so state survives reboots):

  --state-dir .claude/planner-state/T17-$(date +%Y%m%d-%H%M%S)

  Expect the executor to deliver these milestones in order:
  1. src/lib/payments/pesonet.ts — Xendit VA creation client (creates VA per order, returns VA number)
  2. src/lib/payments/webhook-auth.ts — add verifyXenditVaToken or document that the
  existing verifyXenditToken covers it (depends on plan research)
  3. src/features/payments/webhooks/pesonet/route.ts — new route: verify auth → parse VA
  event → normalize to NormalizedWebhookPayload → dispatch to existing
  processPaymentCapture
  4. src/app/api/webhooks/pesonet/route.ts — Next.js app-router mount (re-export from
  features slice)
  5. Order-detail UI: display VA number when Order.paymentMethod === 'PESONET_VA' (or
  equivalent field)
  
  Stop here. Do not open the PR yet.

  Step 7 — TSC check

  npx tsc --noEmit

  Must be clean before proceeding to Session 3. Fix any type errors before ending the
  session.

  ---
  Session 3 — Tests + PR + merge (75 min)

  Goal: Unit + integration tests, CodeRabbit review, merge.

  Step 8 — Resume executor or continue directly

  If resuming via the planner:
  Use your planner skill to execute plans/T-17-pesonet-virtual-account.md
  pointing at the same --state-dir from Session 2.

  Remaining milestones:
  1. Unit tests for verifyXenditVaToken (or whichever verifier was added) — same
  positive/negative/stale-timestamp pattern as webhook-auth.test.ts
  2. normalize.test.ts equivalent for the PESONet payload adapter
  3. handlers-rollback.test.ts equivalent for any new handler (if T-17 adds one; likely
  none since processPaymentCapture is reused unchanged)
  4. Integration test: VA payment webhook → Transaction.status === CAPTURED — same
  pattern as handlers.test.ts
  
  Step 9 — Full test run + TSC

  npx tsc --noEmit && npm test -- --run

  Both must be clean.

  Step 10 — Open PR

  gh pr create --title "feat: T-17 — PESONet virtual account integration" --body "..."

  Step 11 — CodeRabbit

  Wait ~5 minutes. Run:
  gh pr view 14 --comments
  Address any blocking findings, push a fix commit, self-review against the
  Implementation Discipline checklist in CLAUDE.md before merging.

  Step 12 — Merge

  gh pr merge 14 --squash --delete-branch

  ---
  Session budget summary

  ┌─────────┬─────────────────────────────────────────┬─────────────┐
  │ Session │                  Focus                  │    Time     │
  ├─────────┼─────────────────────────────────────────┼─────────────┤
  │ 1       │ Explore + payment research + plan write │ ~90 min     │
  ├─────────┼─────────────────────────────────────────┼─────────────┤
  │ 2       │ Provider client + route + auth + UI     │ ~90 min     │
  ├─────────┼─────────────────────────────────────────┼─────────────┤
  │ 3       │ Tests + PR + CodeRabbit + merge         │ ~75 min     │
  ├─────────┼─────────────────────────────────────────┼─────────────┤
  │ Total   │                                         │ ~4.25 hours │
  └─────────┴─────────────────────────────────────────┴─────────────┘

  The wildcard is the payment-research gate in Session 1 — if Xendit's VA webhook auth
  differs meaningfully from the invoice auth (new header name, HMAC instead of static
  token), the webhook-auth.ts additions expand and push ~15 min into Session 2. Budget an
   extra 30 min if that happens.
