// App router mount point for the service browse RSC.
// Implementation lives in src/features/services/browse/page.tsx per VSA boundary rules (ADR-001).
export { default } from '@/features/services/browse/page'
